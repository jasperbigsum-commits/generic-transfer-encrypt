# Changelog

## 1.0.0

- First Flutter client for the repository transport-encryption protocol.
- Supports encrypted JSON, form, and query requests.
- Supports multipart upload with `__md5_<fieldName>` integrity fields.
- Supports binary download with `X-Transfer-Content-MD5` verification.
